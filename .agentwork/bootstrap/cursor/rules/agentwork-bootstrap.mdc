<!-- AUTO-GENERATED by agentwork bootstrap -->
---
description: agentwork 启动规则（Cursor）
globs: []
alwaysApply: true
---

# agentwork-bootstrap

Cursor 项目规则，来自 agentwork bootstrap 单一源模板。

本仓库协作规范集中在 `.shared/`，此文件为启动时必读的引导规则。

> Cursor 官方更推荐 `.cursor/rules/*.mdc` 项目规则；AGENTS.md 也可用，agentwork 优先生成 rule 文件。

## 启动必读
1. `.shared/constraints/coding-style.md`
2. `.shared/constraints/destructive-operations.md`
3. `.shared/INDEX.md`

## 基本规则
- 默认使用简体中文沟通
- 仅在用户明确要求或必须原样输出代码/命令时使用其他语言
- 高风险操作必须确认
- 不自动加载 `.shared/case/*`
- 不要预读全部 `.shared/project/*.md`；当任务涉及具体项目或目录时，先根据 `.shared/project/index.md` 读取对应的轻量 project 文档
- 当第一次确定将读取或修改的目标路径后，必须立即对照 `.shared/project/index.md` 的读取映射；若命中条目，先读取对应 `project/*.md` 再继续工作；若目标路径扩大，重新对照一次
- 预览未知或可能很大的命令输出时，优先使用 `.shared/scripts/command-preview.sh -- COMMAND ...`，避免直接把大段输出送入上下文
- 临时工件统一写入 `.tmp/`
- 可选工具默认不预装，按项目约定单独引入
- Git index（staged 区）是用户的提交和 review 边界；未经明确要求，不改变 staged 区
- 优先写项目级配置，避免污染全局配置
- 文档任务按需使用 anydoc（详见 `.shared/skills/anydoc/SKILL.md`）：默认提供能力规则，运行时依赖在项目级延迟安装，扫描 PDF 的 hosted OCR 需单独授权。

## 工作流起步建议

- Cursor 没有 agentwork 的斜杠命令入口；按阶段直接读取对应共享命令文件正文并据此执行
- 需求不清晰、边界模糊：先读 `.shared/commands/brain.md`
- 已有方向、需要拆执行步骤：读 `.shared/commands/plan.md`
- 开始做当前批次工作：读 `.shared/commands/exec.md`
- 一轮工作完成后：先读 `.shared/commands/review.md`，再决定是否按 `.shared/commands/commit.md` 提交
- 需要跨会话或跨工具接力任务状态：读 `.shared/commands/case.md`

## 代码导航默认策略

- 处理当前项目内的代码任务时，优先遵循：`.shared/patterns/semantic-navigation.md`
- 对 definition / references / symbols / rename-preview / diagnostics 这类语义查询，助手应默认使用对应语义导航能力
- 路径默认使用当前项目内的相对路径，不写用户目录或绝对工程路径示例
