---
description: "任务需要跨会话或跨工具接力时使用。管理仓库内的显式任务快照：新建、加载、把已确认计划写入快照、按快照推进或审查；旧快照只在显式加载时读取。"
---

<!-- AUTO-GENERATED by agentwork bootstrap -->
# /case [subcommand] [args]

按共享定义执行本命令，参数如下（可为空）：

$ARGUMENTS

## 执行前必读
@.shared/commands/case.md
