---
name: android
description: Android ADB + MCP 命令（双轨验证与回退）
metadata:
  short-description: Android（init + 口语化任务）
---

# /android [subcommand] [args]

Android ADB + MCP 命令（兼容入口，薄封装）。

## 执行前必读

- 主定义：`.shared/commands/android.md`
- 占位符规范：`.shared/constraints/placeholder-naming.md`
