<!-- AUTO-GENERATED by agentwork tool source -->
---
description: arch tool rule for Cursor
globs: []
alwaysApply: false
---

# arch-tool

当任务需要生成或调整架构图时：
- 先读取 `.shared/commands/arch.md`
- 默认维护 `catalog.json` 和 `content/<version>/<item>/` 下的 Markdown、文本图源与 SVG
- 页面路径由 `version + id` 推导，不在 catalog 里重复维护 `href`
- 调整后优先运行 renderer `--check`，必要时读取 `.shared/templates/arch/references/layout-best-practices.md` 和 `portal-workflow.md`
- 正式产物默认放 `docs/architecture/`，临时草稿默认放 `.tmp/architecture/`
- 只生成静态页面和共享 CSS，不引入 server、live reload、搜索、权限或托管预览
