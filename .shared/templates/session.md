# Session: {name}

> 创建: {YYYY-MM-DD HH:MM}
> 简述: {session-desc}

## 任务列表（按优先级）
- [ ] {下一步要做的事}

## 已确认结论（工作快照）
### 目标
- {当前任务目标}
### 边界
- In Scope: {本轮纳入范围}
- Out of Scope: {本轮不做什么}
### 约束
- {约束 1}
### 已选方案
- {当前已确认方案}
### 推荐方案（待确认，可选）
- {尚未确认的推荐方案；确认前不要写成已选方案}
### 待确认问题（可选）
- {需要用户确认后才能进入 /session plan 或 /session exec 的关键问题}
### 核心定义 / 流程（可选）
- {仅保留继续推进所必需的定义或流程}

## 计划摘要（可选）
> 复用 plan 的核心字段；如果计划结构变化，应与 `.shared/templates/plan.md` 保持语义同步。

### 关键文件 / 边界
- {会修改哪些文件、哪些边界不能碰}
### 执行批次 / 优先级
- {当前批次应先做什么}
### 执行策略（可选）
- {standard | ralph}
### 验证策略
- {本轮最小验证方式}
### 完成标准（可选）
- {何时可认为这一轮真正完成}

## 关联工件（可选）
- `.tmp/agentwork/brain/...`
- `.tmp/agentwork/plan/...`
- `.tmp/agentwork/review/...`
- `.tmp/agentwork/ralph/...`

## 当前批次工作集（可选）
- 范围: `src/module/` | 主题: {为什么属于当前批次}
- 范围: `path/to/file` | 主题: {小批次或关键文件可精确列出}
- 范围: `docs/**/*.md` | 主题: {必要时可用 glob；精确路径由 review 脚本/git 取证}

## 产出批次（提交锚点）
- 提交: `abc1234 简要标题` | 范围: `.shared/...`, `src/...`
- 提交: `-` | 范围: `.shared/session/<session-id>.md`（仅用于 session 文件自身这一笔）
- 历史: `{阶段摘要}` | 范围: `{提交区间或主题范围}`（产出过多时合并已闭环批次）

## 风险 / 阻塞
- {当前风险或阻塞}

## 审查记录
### {YYYY-MM-DD HH:MM}
- 变更：{本次里程碑完成项}
- 验证：{检查结果（写结论，不写命令流水）}
- 风险/待办：{剩余风险或下一步}

## 建议摘录到 Project（可选）
- {如果出现长期稳定事实，列在这里等待确认}
