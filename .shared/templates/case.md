# Case: {name}

> 创建: {YYYY-MM-DD HH:MM}
> 简述: {case-desc}

## 任务列表（按优先级）
- [ ] {下一步要做的事}

## 已确认结论（工作快照）
### 目标
- {当前任务目标}
### 边界
- In Scope: {本轮纳入范围}
- Out of Scope: {本轮不做什么}
### 约束
- {约束 1}
### 已选方案
- {当前已确认方案}
### 关键决策 / 取舍（可选）
- {只记录会影响后续恢复的已否决方案、兼容取舍或迁移前提；重复讨论留在 brain/review 工件}
### 推荐方案（待确认，可选）
- {尚未确认的推荐方案；确认前不要写成已选方案}
### 待确认问题（可选）
- {需要用户确认后才能进入 /case plan 或 /case exec 的关键问题}
### 核心定义 / 流程（可选）
- {仅保留继续推进所必需的定义或流程}

## 计划摘要（可选）
> 复用 plan 的核心字段；如果计划结构变化，应与 `.shared/templates/plan.md` 保持语义同步。

### 关键文件 / 边界
- {会修改哪些文件、哪些边界不能碰}
### 执行批次 / 优先级
- {当前批次应先做什么}
### 执行策略（可选）
- {standard}
### 验证策略
- {本轮最小验证方式}
### 完成标准（可选）
- {何时可认为这一轮真正完成}

## 关联工件（可选）
- `.tmp/agentwork/brain/...`
- `.tmp/agentwork/plan/...`
- `.tmp/agentwork/review/...`

## 当前批次工作集（可选）
- 范围: `src/module/` | 主题: {为什么属于当前批次}
- 范围: `path/to/file` | 主题: {小批次或关键文件可精确列出}
- 范围: `docs/**/*.md` | 主题: {必要时可用 glob；精确路径由 review 脚本/git 取证}

## 产出批次（提交锚点）
<!--
保留所有仍有独立行为、决策、验证或风险价值的提交锚点；不要按固定条数截断。
相关提交可聚合在一条“提交:”中，范围优先使用目录/模块/glob，避免罗列所有文件；同一 hash 默认只出现一次，除非验证或边界不同。
每条“提交:”都要写“范围:”和“验证:”。只有 Case/格式/生成物等无新增语义的提交才合并为“历史:”摘要，并保留时间范围和来源锚点。
-->
- 提交: `abc1234 简要标题` | 范围: `.shared/...`, `src/...` | 验证: {结果结论}
- 提交: `-` | 范围: `.shared/case/<case-id>.md` | 验证: Case 快照已与本批次事实同步（仅用于 Case 文件自身这一笔）
- 历史: `{阶段摘要}` | 提交: `abc1234`, `def5678` | 范围: `{提交区间或主题范围}`（仅合并无新增语义的已闭环批次）

## 风险 / 阻塞
- {当前风险或阻塞}

## 审查记录
<!--
先按标题中的实际审查日期识别新旧并按时间倒序维护，不按原文位置推断时间。最近 3 个有独立结论或验证结果的审查事件（不足 3 个则全部）保留“变更 / 验证 / 风险/待办”详情；“历史审查摘要”不计入这 3 个名额；更早事件按里程碑或时间段合并为一句话或简述。
简述仍需保留日期/范围、结论、验证状态、未闭环风险和来源锚点；未解决风险、兼容/迁移前提、关键取舍或接力所需旧结论不因压缩删除。
已关闭或仅作归档指针的 Case 可以主要保留历史摘要，但不得丢失最终事实、验证边界和可追溯锚点。
-->
### {YYYY-MM-DD HH:MM}
- 变更：{本次里程碑完成项}
- 验证：{检查结果（写结论，不写命令流水）}
- 风险/待办：{剩余风险或下一步}

### 历史审查摘要（{历史审查时间范围}）
- {审查日期或主题}：{历史审查一句话摘要；含验证状态、未闭环风险或来源锚点}

## 建议摘录到 Project（可选）
- {如果出现长期稳定事实，列在这里等待确认}
